  #### desc ####
  # this is ruby on rails controller to generate data to yaml files
  # drop ar2yaml_controller.rb to your controller directory & views/index to your views directory
  # just type http://localhost:3000 /ar2yaml/dump/{model_name} on your browser
